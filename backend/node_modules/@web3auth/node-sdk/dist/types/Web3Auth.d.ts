import { type ChainNamespaceType, type CustomChainConfig } from "@web3auth/no-modal";
import { IWeb3Auth, LoginParams, WalletResult, Web3AuthOptions } from "./interface";
export declare class Web3Auth implements IWeb3Auth {
    readonly options: Web3AuthOptions;
    private torusUtils;
    private nodeDetailManager;
    private analytics;
    private projectConfig;
    constructor(options: Web3AuthOptions);
    get currentChainId(): string;
    get currentChainNamespace(): ChainNamespaceType;
    get currentChain(): CustomChainConfig;
    init(): Promise<void>;
    connect(loginParams: LoginParams): Promise<WalletResult>;
    private getUseDKGDefaultValue;
    private validateConstructorOptions;
    private initChainsConfig;
    private getTorusKey;
    private getOAuthProviderConfig;
    private getUserId;
    private getWallet;
    private getInitializationTrackData;
}
